public class MyActor
{
    public void DoLog(string toLog)
    {
        /*var logger = SFIOC.IoCFW.Globals.GetBinding<ISuperLogger>();
        var bindedTo = SFIOC.IoCFW.Globals.SearchBindings<MyCustomLogger>();

        logger.Log("Searching binding...");
        foreach (var value in bindedTo)
        {
            logger.Log($"Found binding: {value.Name}, interface? {value.IsInterface}");
        }*/
    }
}